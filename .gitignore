# Environment variables
.env

# Local choice of dashboard user (see scripts/deploy.sh)
.dashboard-user

# Persistent data (includes data/vault/, the Obsidian second-brain vault; never commit)
data/

# OS / Editor files
.DS_Store
*.log
